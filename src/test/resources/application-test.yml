spring:
  datasource:
    url: jdbc:h2:mem:${random.uuid};MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
      schema-locations: classpath:schema-h2.sql
  flyway:
    enabled: false
  data:
    redis:
      url: redis://localhost:6379

auth:
  jwt:
    secret: test-only-jwt-secret-32-byte-key!
    issuer: bike-back-test
    token-validity-sec: 900

bike:
  rate-limit:
    store: memory
